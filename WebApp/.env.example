user=your_user
password=your_password

# Public client-visible version (prefix with NEXT_PUBLIC_ to expose to browser)
NEXT_PUBLIC_VERSION=v1.0.0

# Secret used to sign session tokens (keep private)
AUTH_SECRET=replace_this_with_a_random_string

# MySQL configuration (optional). If set, the app will use MySQL to store PowerPC state.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=example
DB_NAME=name_of_your_database